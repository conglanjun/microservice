spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:15432/postgres
  sql:
    init:
      mode: always
      platform: postgres
  data:
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      timeout: 1000

